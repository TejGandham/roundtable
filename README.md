# roundtable — distribution

Public distribution point for **roundtable** release binaries (per-platform
archives + `SHA256SUMS`). See the [Releases](../../releases) page.

The source code is maintained privately. This repository carries published
release artifacts only — no source tree.

Install instructions: see the release notes / `INSTALL.md` shipped with each release.
