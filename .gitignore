# Build artifacts
/_build/
/deps/
/roundtable

# Release binaries uploaded to GitHub/Forgejo, not tracked in git
/release/roundtable-*

# Sisyphus evidence (noisy, not needed in git)
.sisyphus/evidence/
.sisyphus/boulder.json

# Local workflow state
.sisyphus/
.letta/
erl_crash.dump
.claude/
.codex
SHA256SUMS

# Built binaries
/roundtable-cli
.depwire/
roundtable-0.8.0-*.tar.gz
