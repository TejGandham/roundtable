# Build artifacts
/_build/
/deps/
/roundtable

# Track release binary (exception to above)
!/release/roundtable

# Sisyphus evidence (noisy, not needed in git)
.sisyphus/evidence/
.sisyphus/boulder.json

# Local workflow state
.sisyphus/
.letta/
erl_crash.dump

# Built escript binary
/roundtable-cli
