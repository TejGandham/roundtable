# Build artifacts
/_build/
/deps/
/roundtable

# Release binaries uploaded to GitHub/Forgejo, not tracked in git
/release/roundtable-*

# Sisyphus evidence (noisy, not needed in git)
.sisyphus/evidence/
.sisyphus/boulder.json

# Local workflow state
.sisyphus/
.letta/
.superpowers/
docs/superpowers/
erl_crash.dump
# KEEL framework: track the install per docs/INSTALL.md ("committed to your repo").
# `.claude/*` ignores immediate children so the re-includes below take effect;
# `settings.local.json` stays ignored as personal Claude Code state.
.claude/*
!.claude/agents
!.claude/skills
!.claude/hooks
!.claude/settings.json
!.claude/.keel-install.json
!.claude/keel-uninstall.py
!.claude/keel_manifest.py
!.claude/keel_receipt.py
!.claude/keel_settings.py
.codex
SHA256SUMS

# Built binaries
/roundtable-cli
.depwire/
# Release archives built by `make release` + packaging step — uploaded to
# GitHub/Forgejo releases, not tracked in git. Covers any version.
roundtable-*.tar.gz
roundtable-*.zip
