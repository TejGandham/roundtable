#!/bin/sh

# MCP server runs standalone — no distributed Erlang, no epmd.
export RELEASE_DISTRIBUTION=none

# Write crash dumps to the release directory, not the user's working directory.
export ERL_CRASH_DUMP="${RELEASE_ROOT}/erl_crash.dump"
